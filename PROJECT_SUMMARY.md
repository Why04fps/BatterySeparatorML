# 项目总结（简历 / 面试用）

## 项目名称
锂电池隔膜性能机器学习预测与筛选工具

## 一句话概括
用 RDKit 分子描述符 + XGBoost 构建机器学习模型，从聚合物分子结构快速预测隔膜关键性能（Tg、Td、离子电导率），将传统数月实验筛选缩短为秒级预测，并开发了 Streamlit 网页筛选工具。

## 项目目标
隔膜是锂电池最关键的部件之一，性能直接决定电池安全。传统依赖实验筛选候选材料，周期长（数月）、效率低。本项目通过机器学习从聚合物分子结构（PSMILES）快速预测隔膜性能，加速材料筛选。

## 方法

### 数据
- 从多个公开数据集整理约 2.2 万条聚合物/电解质样本，覆盖 3 个性能目标
- **数据增强**：5 条人工负样本（教会模型"什么结构不导电"）+ 12 条文献补充数据（修正盐类型排序与含氟聚合物偏差）

### 特征工程（两套体系）
1. **纯聚合物本征性能（Tg/Td）**：RDKit 从 SMILES 计算 12 个分子描述符（分子量、环数、可旋转键、TPSA、氢键供/受体等）
2. **聚合物电解质电导率（多组分）**：
   - RDKit 描述符 + 盐类型混合编码（Top5 one-hot + 频率编码 + 归并）+ 盐浓度 + 温度
   - **物理约束特征（VFT 理论）**：`Tg`、`T_minus_Tg`、`is_glassy`
   - **硬规则特征**：`has_heteroatom`（纯烃链无法络合锂盐 → 本征不导电）

### 建模
- XGBoost 回归 + GridSearchCV 超参数调优 + 5 折交叉验证
- 早停机制（`early_stopping_rounds=50`）基于独立验证集，避免数据泄漏
- 电导率采用 log 尺度建模（因跨度达 15 个数量级）
- 人工负样本强制留在训练集（`is_synthetic` 标记），避免污染测试评估

### 可解释性
- SHAP 蜂群图分析特征贡献，验证模型学到的规律与高分子物理/电化学理论一致
- `T_minus_Tg` 成为电导率第 2 重要特征（VFT 理论验证）

## 结果

| 目标 | 测试 R² | MAE | 关键特征 |
|------|---------|-----|----------|
| Tg（玻璃化转变温度） | 0.842 | 32.5 °C | 环数（环结构↑刚性↑Tg） |
| Td（热分解温度） | 0.667 | 39.7 °C | 环/芳香环（↑耐热） |
| 离子电导率(log) | 0.871 | 0.48 log | 分子量+T-Tg+盐类型+杂原子 |

## 工程落地
- 模块化代码架构（config/features/train/predict/shap 分层）
- Streamlit 网页工具：输入分子结构即可预测，支持批量筛选与结果导出，含"模型适用范围"提示
- 完整的数据清洗管线（`prepare_data.py`）与外部验证脚本（`run_external_validation.py`）

## 局限性（诚实陈述）
1. **氧化稳定性目标**：因数据缺失暂未建模（4 个目标中的第 4 个）
2. **泛化边界**：模型适用范围为"含杂原子极性聚合物 + 锂盐 + 25-80°C"；纯烃链（PE/PP/PS）经硬规则修正为低电导率但缺真实数据验证，含氟聚合物（PVDF）训练样本极少、预测为外推
3. 特征维度有限（12 个描述符），未纳入 Li:EO 比等电导率细分特征

## 技术栈
Python · RDKit · XGBoost · scikit-learn · SHAP · Streamlit · pandas

## 面试可强调的亮点
1. **领域知识 + ML 结合**：特征设计有化学物理依据（VFT 理论、离子传导机制），模型结果与高分子/电化学理论互相印证
2. **严谨的验证意识**：不仅做了内部交叉验证，还主动引入外部数据集（Table S3）检验，发现并诚实记录泛化边界
3. **多目标、多特征体系**：能区分"纯聚合物本征性能"与"多组分条件性能"两类问题的不同建模方式
4. **物理约束注入**：人工构造负样本 + 硬规则特征（has_heteroatom），解决模型外推错误的实际问题
5. **完整工程闭环**：从数据清洗 → 训练 → 可解释性 → 网页部署全流程独立完成
