# 锂电池隔膜筛选工具依赖
# RDKit 推荐通过 conda 安装（conda install -c conda-forge rdkit），其余用 pip

xgboost>=1.7.0
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
matplotlib>=3.6.0
openpyxl>=3.0.0
streamlit>=1.30.0
joblib>=1.2.0
shap>=0.44.0

# RDKit（分子描述符计算，推荐 conda 安装）
# conda install -c conda-forge rdkit
rdkit>=2023.3.1
